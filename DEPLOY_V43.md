# v4.3 配置手順（まだ実行しなくてよい）

1. Supabaseでプロジェクト作成
2. SQL migrationを適用
3. Authのメールログイン設定
4. Project URLとpublishable keyをapp_config.jsへ設定
5. Edge Functionsをdeploy
6. Edge Function Secretsへ外部APIキーを登録
7. PWAをHTTPS静的ホスティング
8. ログイン → クラウド保存 → 別ブラウザ復元を確認

現時点では箱だけ作成済み。
実アカウント作成・本番デプロイはまだ行っていない。
